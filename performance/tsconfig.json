{
  "compilerOptions": {
    "target": "ES2015",
    "module": "ES2015",
    "lib": ["ES2015"],
    "strict": true,
    "moduleResolution": "bundler",
    "types": ["k6"]
  },
  "include": [
    "scenarios/**/*.ts",
    "clients/**/*.ts",
    "helpers/**/*.ts",
    "options/**/*.ts"
  ],
  "exclude": ["reports/**/*.ts"] // report generator runs in Node, not k6 - separate config
}
